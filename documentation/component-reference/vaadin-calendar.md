# VaadinCalendar

{@code VaadinCalendar} – server-side Vaadin Flow component wrapping the {@code <vaadin-calendar>} web element powered by FullCalendar 6 (MIT).

## Class

- **Package:** `com.holonplatform.vaadin.flow.calendar`
- **Source:** `calendar/src/main/java/com/holonplatform/vaadin/flow/calendar/VaadinCalendar.java`
- **Signature:** `public class VaadinCalendar extends Component implements HasSize, Focusable<VaadinCalendar>, LocaleChangeObserver`

## Key APIs

### Common methods

- `String getHex()`
- `String getTextHex()`
- `String getLabel()`
- `String getId()`
- `String getName()`
- `String getColor()`
- `boolean isVisible()`
- `CalendarGroup withVisible(boolean v)`
- `String toJson()`
- `String getId()`
- `String getTitle()`
- `LocalDateTime getStart()`

## Usage

```java
VaadinCalendar component; // See source for constructor/builder options
```
